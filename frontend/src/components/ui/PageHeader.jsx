import React from'react';
import { Link} from'react-router-dom';
import { ChevronRight} from'lucide-react';
import { cn} from'../../lib/utils';
/** * PageHeader - Consistent page header with breadcrumbs, title, and actions * * Provides a standardized header pattern across all pages with optional breadcrumbs * and action buttons. * * @example * <PageHeader * breadcrumbs={[ * { label:'Jobs', href:'/jobs'}, * { label:'Active Jobs'} * ]} * title="Active Jobs" * subtitle="Manage all active construction projects" * action={<Button>New Job</Button>} * /> */
export function PageHeader({ breadcrumbs = [], title, subtitle, action, className, ...props}) { return ( <div className={cn("mb-8", className)} {...props}> {/* Breadcrumbs */} {breadcrumbs.length > 0 && ( <nav className="flex items-center gap-2 mb-3"> {breadcrumbs.map((crumb, index) => ( <React.Fragment key={index}> {crumb.href ? ( <Link to={crumb.href} className="text-xs font-mono text-gray-500 hover:text-white transition-colors duration-150" > {crumb.label} </Link> ) : ( <span className="text-xs font-mono text-gray-500"> {crumb.label} </span> )} {index < breadcrumbs.length - 1 && ( <ChevronRight className="w-3 h-3 text-gray-600" /> )} </React.Fragment> ))} </nav> )} {/* Title and Action Row */} <div className="flex items-start justify-between gap-4"> <div> <h1 className="text-4xl font-semibold text-white mb-2"> {title} </h1> {subtitle && ( <p className="text-sm text-gray-400"> {subtitle} </p> )} </div> {action && ( <div className="flex-shrink-0"> {action} </div> )} </div> </div> );}
export default PageHeader;
