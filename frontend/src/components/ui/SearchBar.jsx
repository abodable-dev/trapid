import React from'react';
import { Search, X} from'lucide-react';
import { cn} from'../../lib/utils';
/** * SearchBar - Consistent search input with icon and clear functionality * * Provides a standardized search pattern across list pages with optional * clear button and placeholder customization. * * @example * <SearchBar * value={searchQuery} * onChange={(e) => setSearchQuery(e.target.value)} * placeholder="Search contacts..." * onClear={() => setSearchQuery('')} * /> */
export function SearchBar({ value ='', onChange, onClear, placeholder ='Search...', className, ...props}) { const handleClear = () => { if (onClear) { onClear();}}; return ( <div className={cn("relative", className)}> {/* Search Icon */} <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-500" /> {/* Input */} <input type="text" value={value} onChange={onChange} placeholder={placeholder} className={cn("w-full pl-10 pr-10 py-2","bg-gray-900 border border-[#272727]","text-sm text-white placeholder:text-gray-500","focus:outline-none focus:border-gray-600","transition-colors duration-150" )} {...props} /> {/* Clear Button */} {value && onClear && ( <button type="button" onClick={handleClear} className={cn("absolute right-3 top-1/2 -translate-y-1/2","text-gray-500 hover:text-white","transition-colors duration-150" )} > <X className="w-4 h-4" /> </button> )} </div> );}
export default SearchBar;
